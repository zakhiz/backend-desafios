
<% if (users.length > 0) { %>
    <% users.forEach(user => { %>
        <p><%=user.name%></p>
        <p><%=user.last_name%></p>
        <p><%=user.age%></p>
        <br>
    <% }) %>
<% } else { %>
   <p>No hay Usuarios registrados</p>
<% } %> 

